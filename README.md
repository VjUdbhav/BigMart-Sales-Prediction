# BigMart-Sales-Prediction
This repository is for a project on Bigmart Sales Prediction Model using XGBoost regressor. It uses multiple features of the training dataset, to predict the target variable 'Item_Outlet_Sales' for the test dataset.
